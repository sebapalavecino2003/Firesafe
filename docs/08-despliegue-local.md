# DESPLIEGUE LOCAL

El sistema se ejecuta localmente mediante Docker Compose.

Comandos principales:

- docker compose build
- docker compose up

Cada servicio expone documentación Swagger para validación.
